{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "pretest": "npm ci && cd .. && docker exec -i database createdb -U admin tawjih_test 2>/dev/null || true && docker compose run --rm -e DATABASE_URL=postgresql://admin:adminpassword@db:5432/tawjih_test migrator node migrate.js",
    "test": "NODE_ENV=test jest --runInBand --forceExit",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "compression": "^1.7.4",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.22.3",
    "express-rate-limit": "^7.4.0",
    "express-validator": "^7.2.1",
    "google-auth-library": "^11.0.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.12.0",
    "multer": "^2.2.0",
    "pg": "^8.11.5",
    "resend": "^6.20.0",
    "winston": "^3.13.0"
  },
  "devDependencies": {
    "jest": "^30.4.2",
    "supertest": "^7.2.2"
  }
}
